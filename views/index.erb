<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Floating Point Types in Ruby</title>

  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css">
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap-theme.min.css">

  <style>
    td.pass {
      background-color: LightGreen;
    }
    td.fail {
      background-color: Salmon;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <h1>Floating Point Types in Ruby</h1>

        <p>A series of accuracy tests that demonstrate the problems with Floating
          point values. The same examples are run with each of Ruby's Float,
          Rational and BigDecimal types to compare their behaviour.</p>
        <p>Issues to note:</p>
        <ul>
          <li>Just because the Rational and BigDecimal types appear to pass a test
            when fed numbers that make Float fail, doesn't prove there aren't different
            numbers that would make these two types fail.</li>
          <li>The 'Difference' values may not be accurate.</li>
          <li>The floating point values may be being rounded.</li>
        </ul>


        <% @results.each do |test, test_results| %>

        <h2><%= test %></h2>

          <table class="table">
            <thead>
              <th width="10%">Type</th>
              <th width="10%">Result</th>
              <th width="26%">Expected</th>
              <th width="26%">Got</th>
              <th width="28%">Difference</th>
            </thead>
            <% test_results.each do |test_result| %>

            <tr>
              <td><%= test_result.type %></td>
              <td class="<%= test_result.result %>"><%= test_result.result %></td>
              <td><%= test_result.info[:expected] %></td>
              <td><%= test_result.info[:got] %></td>
              <td><%= test_result.info[:delta] %></td>
            </tr>
            <% end %>
          </table>

        <% end %>
      </div>
    </div>
  </div>

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
  <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
</body>
</html>